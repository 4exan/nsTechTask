spring:
  application:
    name: "tech-task"
  datasource:
    url: jdbc:h2:file:./db
    username: u
    password: 1
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2-console
